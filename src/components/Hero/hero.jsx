export default function Hero({fuson}){
    return(
        <div style={{
            background: fuson ? "white" : "black"
        }} id="hero">
            <div className="container">
                <div className="hero">
                    
                </div>
            </div>
        </div>
    )
}